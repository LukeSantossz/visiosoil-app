# Enforcement Configuration — Debug Log Patterns
#
# Padrões de debug log por linguagem que o pre-commit hook deve detectar
# nos arquivos staged. Uma linha por padrão, formato: EXTENSÃO:REGEX
#
# O hook bloqueia commits que contenham esses padrões em arquivos staged.
# Use com moderação — falsos positivos que impedem o trabalho são piores
# que falsos negativos.

# Dart / Flutter
dart:print\(
dart:debugPrint\(
dart:log\(
dart:// TODO
dart:// FIXME
dart:// HACK

# Genérico (todas as extensões)
*:<<<<<<<
*:=======
*:>>>>>>>
